import 'package:flutter/material.dart';

class CustomExchangeDialog extends StatelessWidget {
  const CustomExchangeDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
